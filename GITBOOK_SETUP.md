# GitBook Setup Guide

Документация WinFlow теперь использует GitBook для лучшей организации и навигации.

## Что изменилось?

### Было (GitHub Pages)
- Jekyll тема `just-the-docs`
- Конфигурация в `_config.yml`
- Статические HTML страницы
- Ограниченная навигация

### Стало (GitBook)
- Современный интерфейс с поиском
- Структурированная навигация через `SUMMARY.md`
- Конфигурация в `.gitbook.yaml`
- Интеграция с GitHub
- Мобильная версия
- Встроенный поиск по всему контенту

## Структура документации

```
WinFlow/
├── .gitbook.yaml          # GitBook конфигурация
├── README.md              # Главная страница
├── SUMMARY.md             # Оглавление (структура книги)
└── docs/                  # Документация
    ├── quickstart.md
    ├── language.md
    ├── functions.md
    ├── commands.md
    ├── advanced.md
    ├── cli.md
    ├── examples.md
    ├── install.md
    ├── installer.md
    ├── association.md
    ├── faq.md
    ├── changelog.md
    ├── roadmap.md
    ├── contributing.md
    └── development.md
```

## Настройка GitBook

### Вариант 1: GitBook Cloud (рекомендуется)

1. Перейти на [gitbook.com](https://www.gitbook.com/)
2. Sign up / Sign in с GitHub аккаунтом
3. Создать новое пространство (Space)
4. Выбрать "Import from GitHub"
5. Выбрать репозиторий `silasWorked/WinFlow`
6. GitBook автоматически распознает `.gitbook.yaml` и `SUMMARY.md`
7. Опубликовать документацию

**Результат:**
- Автоматическая синхронизация с GitHub
- Публичный URL: `https://winflow.gitbook.io/` (или кастомный домен)
- Автоматическая сборка при push в main

### Вариант 2: GitBook CLI (локальная разработка)

```powershell
# Установить GitBook CLI
npm install -g gitbook-cli

# Инициализировать (уже сделано)
# gitbook init

# Локальный preview
cd C:\Users\silas\RiderProjects\WinFlow
gitbook serve

# Откроется http://localhost:4000
```

### Вариант 3: GitBook GitHub Integration

1. В репозитории GitHub → Settings → Integrations
2. Найти "GitBook" в GitHub Marketplace
3. Установить интеграцию
4. Авторизовать доступ к репозиторию
5. GitBook автоматически создаст Space и настроит синхронизацию

## Обновление документации

### Локальные изменения

```powershell
# 1. Редактировать .md файлы в docs/
code docs/language.md

# 2. Обновить SUMMARY.md при добавлении новых страниц
code SUMMARY.md

# 3. Commit и push
git add docs/
git commit -m "docs: Update language documentation"
git push

# 4. GitBook автоматически обновится (при настроенной интеграции)
```

### Добавление новой страницы

**1. Создать файл:**
```powershell
New-Item docs/new-feature.md -ItemType File
```

**2. Добавить контент:**
```markdown
# New Feature

Description of the new feature...

## Examples

```wflow
// Example code
```
```

**3. Добавить в SUMMARY.md:**
```markdown
## Продвинутые возможности

* [Functions](docs/functions.md)
* [New Feature](docs/new-feature.md)  <!-- Добавить сюда -->
* [Advanced](docs/advanced.md)
```

**4. Commit:**
```powershell
git add docs/new-feature.md SUMMARY.md
git commit -m "docs: Add new feature documentation"
git push
```

## Настройка .gitbook.yaml

```yaml
root: ./

structure:
  readme: README.md       # Главная страница
  summary: SUMMARY.md     # Оглавление

redirects:
  # Перенаправления со старых URL
  docs/overview.md: docs/quickstart.md
```

### Дополнительные опции

```yaml
# Добавить в .gitbook.yaml для кастомизации

# Плагины
plugins:
  - search
  - copy-code-button
  - github

# GitHub
github:
  repository: silasWorked/WinFlow
  branch: main
```

## SUMMARY.md структура

```markdown
# Summary

## Введение
* [О проекте](README.md)
* [Быстрый старт](docs/quickstart.md)

## Основы
* [Язык WinFlow](docs/language.md)
  * [Синтаксис](docs/language.md#синтаксис)
  * [Переменные](docs/language.md#переменные)

## Продвинутые возможности
* [Функции](docs/functions.md)

## Справочник
* [FAQ](docs/faq.md)
```

**Правила:**
- `*` для пунктов меню
- `  *` для подпунктов (вложенность 2 пробела)
- Ссылки относительно корня проекта
- Якоря для секций: `#заголовок` (lowercase, spaces → `-`)

## Преимущества GitBook

### Для пользователей
✅ **Поиск**: Полнотекстовый поиск по всей документации  
✅ **Навигация**: Древовидная структура с автооткрытием  
✅ **Мобильная версия**: Адаптивный дизайн  
✅ **Темная тема**: Автоматическая поддержка  
✅ **Быстрая загрузка**: Оптимизированные страницы  

### Для разработчиков
✅ **Markdown**: Нативная поддержка .md  
✅ **Git sync**: Автосинхронизация с GitHub  
✅ **Версионирование**: Документация для разных версий  
✅ **Коллаборация**: Многопользовательское редактирование  
✅ **Preview**: Локальный просмотр изменений  

## Публикация

### GitBook Cloud

После настройки интеграции:

1. **Автоматическая публикация** при push в `main`
2. **Public URL**: `https://winflow.gitbook.io/`
3. **Custom domain**: настроить в GitBook Dashboard

### GitHub Pages (альтернатива)

GitBook может экспортировать статический HTML:

```powershell
# Собрать статику
gitbook build

# Результат в _book/
# Опубликовать в gh-pages branch

git checkout -b gh-pages
git add _book
git commit -m "docs: Build GitBook site"
git subtree push --prefix _book origin gh-pages
```

## Миграция завершена ✅

- ✅ Создана структура GitBook
- ✅ Переписан README.md с современными примерами
- ✅ Добавлены новые страницы (quickstart, functions, changelog, roadmap, etc.)
- ✅ Обновлена документация языка (language.md)
- ✅ Удалены старые конфиги GitHub Pages
- ✅ Все изменения закоммичены и запушены

## Следующие шаги

1. **Настроить GitBook Cloud** для автоматической публикации
2. **Добавить badge** в README с ссылкой на документацию
3. **Обновить Wiki** (если используется) со ссылкой на GitBook
4. **Анонсировать** обновление документации в Releases

---

**Документация теперь организована и готова к использованию с GitBook!** 📚
