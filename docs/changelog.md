# Changelog

История изменений проекта WinFlow.

## [0.2.0] - 2024-12-18

## [0.2.6] - 2025-12-19

### 🎯 Added ✨
- **Installer UI (WinForms)** — graphical installer to manage install/uninstall and options interactively
- **InstallerService** — shared backend logic for install/uninstall operations and progress reporting
- **Installer CLI flags**: `--register-assoc`, `--unregister-assoc`, `--assoc-target` for file association management (per-user)
- **Context menu verbs**: Run with WinFlow / Debug with WinFlow (registered for `.wflow` files)
- **Tests**: add integration tests for installer association roundtrip

### 🔄 Changed
- Improve installer error handling and logging
- Add publishing GitHub Action to produce installer artifact (single-file EXE)

---

## [0.2.3] - 2024-12-18

> Фактическая версия CLI/Runtime: 0.2.5

### 🎯 Добавлено ✨

**7 новых модулей:**
- **config** — работа с INI конфигурациями (create, read, get, set, write)
- **csv** — полнофункциональная работа с CSV таблицами (read, create, add_row, filter, sort, write)
- **xml** — парсинг и генерация XML документов (parse, create, add_element, set_attribute, get, write)
- **registry** — улучшенная работа с Windows реестром (get, set, exists, delete с дружественными дефолтами)
- **async** — асинхронное выполнение команд (start, wait, wait_all, status)
- **input** — интерактивный ввод данных (text, password, confirm, choice)
- **import** — загрузка функций из модулей без выполнения верхнеуровневого кода

**Архитектурные улучшения:**
- **Object Store** в ExecutionContext для управления сложными объектами
- **Асинхронный реестр** для фоновых задач
- Двойная изоляция контекстов (параметры функций + объекты)

**Расширения существующих модулей:**
- datetime: кастомные форматы, парсинг, вычисление разности
- path: basename, extension, join(parts)
- log: структурированное логирование (log.config + info/debug/warning/error)

**Полный набор тестов:**
- 9 новых .wflow файлов в `/tests`
- Интерактивный раннер `test-master-inputs.wflow`
- Примеры для каждого модуля

### 🔄 Изменено
- Инсталлятор теперь закрывает запущенные процессы перед обновлением (с запросом)
- CLI автоматически проверяет обновления при запуске
- Красивый вывод информации об обновлениях в цветном форматировании

### 🧪 Тестирование
Все новые модули протестированы:
- test-config.wflow — конфигурации INI
- test-csv.wflow — таблицы с фильтрацией и сортировкой
- test-xml.wflow — парсинг и генерация XML
- test-registry.wflow — работа с реестром HKCU
- test-async.wflow — параллельные задачи
- test-input.wflow — интерактивные вводы
- test-import.wflow — импорт модулей

### 📊 Метрики
- +1500 строк кода в CommandDispatcher
- +70 строк в инсталлятор (процесс закрытие)
- +84 строк в CLI (автопроверка обновлений)
- 0 breaking changes

---

## [0.2.0] - 2024-12-18

### Добавлено ✨
- **Функции как в настоящих языках программирования**
  - Многострочные определения: `define funcname(param1, param2):`
  - Тела функций с отступами (4 пробела или tab)
  - Прямой вызов функций: `funcname(arg1, arg2)`
  - Позиционная передача параметров
- **Локальная область видимости**
  - Параметры функций локальные (не влияют на глобальные переменные)
  - Изоляция контекста выполнения
  - Обратная пропагация изменений неглобальных переменных
- **Глобальный реестр функций**
  - Static Dictionary в CommandDispatcher
  - Функции персистентны между выполнениями
  - Двухпроходный парсинг (сначала функции, потом команды)

### Изменено 🔄
- **Парсер (WinFlowParser.cs)**
  - Полностью переписан для поддержки многострочных блоков
  - Добавлены методы: `ParseFunctions()`, `ParseFunctionDef()`, `ParseFunctionCallArgs()`
  - Распознавание вызовов функций через паттерн `name(...)`
- **CommandDispatcher.cs**
  - Добавлен статический `GlobalFunctions` Dictionary
  - Реализован метод `RegisterFunction()`
  - Обновлен обработчик `call` для поддержки параметров
- **Модели данных**
  - FlowFunction: добавлены Properties (Name, Parameters, Commands)
  - FlowCommand: добавлено поле Metadata
  - ExecutionContext: Functions теперь Dictionary<string, FlowFunction>

### Исправлено 🐛
- Функции больше не теряются при выполнении
- Параметры не загрязняют глобальное окружение
- Корректная подстановка переменных в телах функций

### Тесты ✅
- `test-greet2.wflow` — функция с одним параметром
- `test-functions-v2.wflow` — функции с несколькими параметрами
- Все тесты проходят успешно

---

## [0.1.9] - 2024-12-17

### Добавлено ✨
- **JSON модуль**
  - `json.parse` — парсинг JSON файлов
  - `json.get` — получение значений по пути
  - `json.set` — установка значений
- **HTTP модуль (Net)**
  - `net download` — загрузка файлов
  - `net request` — HTTP запросы (GET, POST, etc.)
- **Массивы (Array)**
  - `array.create` — создание массивов
  - `array.add` — добавление элементов
  - `array.get` — получение элементов
  - `array.length` — размер массива
- **Обработка ошибок**
  - `try:` / `catch:` блоки
  - Перехват исключений в командах
- **Базовые функции**
  - `define name=... body=...` — определение функций (старый синтаксис)
  - `call name=...` — вызов функций (старый синтаксис)

### Тесты ✅
- `test-v019.wflow` — JSON и HTTP
- `test-v019-all.wflow` — все возможности v0.1.9
- `test-try-catch.wflow` — обработка ошибок

---

## [0.1.8] - 2024-12-16

### Добавлено ✨
- **Циклы**
  - `loop.repeat count=N:` — повтор N раз с многострочным телом
  - `loop.foreach array=... element=...:` — итерация по массиву
  - Переменные цикла: `${index}`, `${element}`
- **Условия**
  - `if condition=... equals=...:` — условный блок
  - `else:` — альтернативный блок
  - Операторы: equals, not_equals, greater, less, contains
- **Многострочные блоки**
  - Поддержка блоков с отступами для циклов и условий
  - Парсинг вложенных структур

### Изменено 🔄
- Парсер обновлен для поддержки блочных структур
- CommandDispatcher расширен обработчиками циклов и условий

### Тесты ✅
- `test-v017.wflow` — циклы и условия

---

## [0.1.7] - 2024-12-15

### Добавлено ✨
- **Модуль Process**
  - `process.exec` — синхронный запуск процессов
  - `process.start` — асинхронный запуск
  - Переменная `${LASTEXITCODE}` — код возврата последнего процесса
- **Модуль File (расширение)**
  - `file exists` — проверка существования
  - `file read` — чтение содержимого в переменную `${CONTENT}`
  - `file mkdir` — создание директорий
- **Модуль String**
  - `string.replace`, `string.contains`, `string.length`
  - `string.upper`, `string.lower`, `string.trim`

### Тесты ✅
- `test-process.wflow` — запуск процессов
- `demo.wflow` — комплексное демо

---

## [0.1.6] - 2024-12-14

### Добавлено ✨
- **Модуль File**
  - `file write` — запись файлов
  - `file append` — добавление к файлам
  - `file copy` — копирование
  - `file move` — перемещение
  - `file delete` — удаление
- **Модуль Env**
  - `env set` — установка переменных
  - `env get` — получение переменных
  - `env print` — вывод всех переменных
  - `env unset` — удаление переменных
- **Подстановка переменных**
  - Синтаксис `${VAR_NAME}`
  - Работа во всех командах

### Тесты ✅
- `test-var-expansion.wflow` — подстановка переменных

---

## [0.1.5] - 2024-12-13

### Добавлено ✨
- **CLI параметры**
  - `--verbose` — подробный вывод
  - `--dry-run` — режим без выполнения
  - `--version` — версия приложения
- **Базовые команды**
  - `echo` — вывод текста
  - `noop` — пустая операция
  - `exit` — выход с кодом

### Изменено 🔄
- Рефакторинг архитектуры: AST, Parser, Runtime разделены
- CommandDispatcher реализован с поддержкой расширений

---

## [0.1.0] - 2024-12-10

### Начальная версия 🎉
- Создан проект WinFlow
- Базовая структура решения (.NET 8)
- Минимальный парсер и рантайм
- CI/CD workflow (GitHub Actions)
- Документация (GitHub Pages)

---

## Легенда

- ✨ **Добавлено** — новые возможности
- 🔄 **Изменено** — изменения в существующей функциональности
- 🐛 **Исправлено** — исправления багов
- ❌ **Удалено** — удаленная функциональность
- 🔒 **Безопасность** — исправления безопасности
- ✅ **Тесты** — добавленные/обновленные тесты

---

## Ссылки

- [Releases](https://github.com/silasWorked/WinFlow/releases)
- [Roadmap](roadmap.md)
- [Contributing](contributing.md)
