# WinFlow v0.2.3 Release Notes

**Версия (релиз):** 0.2.3  
**Версия CLI/Runtime:** 0.2.5  
**Дата выпуска:** 18 декабря 2025  
**Статус:** Стабильная

---

## 🎯 Что нового в v0.2.3

Масштабное расширение функциональности: **7 новых модулей**, **объектное хранилище**, **асинхронное выполнение** и полный набор утилит для работы с данными и системой.

---

## 📦 Новые модули

### 1. **config** — INI конфигурации
Работа с конфигурационными файлами формата INI.

```wflow
config.create var=CFG
config.set config=${CFG} section=App key=Name value=MyApp
config.get config=${CFG} section=App key=Name var=APP_NAME
config.write config=${CFG} file=settings.ini
config.read file=settings.ini var=CFG
```

**Команды:**
- `config.create` — создать новую конфигурацию
- `config.read file=<path>` — прочитать INI файл
- `config.get config=<id> section=<name> key=<name>` — получить значение
- `config.set config=<id> section=<name> key=<name> value=<val>` — установить значение
- `config.write config=<id> file=<path>` — сохранить в файл

---

### 2. **csv** — Работа с CSV данными
Чтение, создание, фильтрация и сортировка CSV таблиц.

```wflow
csv.create headers="name,email,age" var=PEOPLE
csv.add_row data=${PEOPLE} values="Alice,alice@mail.com,30"
csv.add_row data=${PEOPLE} values="Bob,bob@mail.com,25"
csv.filter data=${PEOPLE} column=age value=30 var=ALICE
csv.sort data=${PEOPLE} column=age order=desc var=SORTED
csv.write data=${PEOPLE} file=people.csv
csv.read file=people.csv has_header=true var=DATA
```

**Команды:**
- `csv.create headers=<a,b,c>` — создать таблицу
- `csv.read file=<path> [has_header=true]` — прочитать CSV
- `csv.add_row data=<id> values=<v1,v2>` — добавить строку
- `csv.get_field row=<rowId> field=<name>` — получить ячейку
- `csv.filter data=<id> column=<name> value=<val>` — отфильтровать
- `csv.sort data=<id> column=<name> [order=asc|desc]` — отсортировать
- `csv.write data=<id> file=<path>` — сохранить CSV

---

### 3. **xml** — Работа с XML документами
Парсинг, создание и модификация XML.

```wflow
xml.parse file=data.xml var=DOC
xml.get data=${DOC} xpath="/root/user/name" var=NAME
xml.create var=XML
xml.add_element parent=${XML} tag=root var=ROOT
xml.set_attribute element=${ROOT} name=id value=1
xml.write data=${XML} file=output.xml
```

**Команды:**
- `xml.create` — создать XML документ
- `xml.parse file=<path>` — прочитать XML
- `xml.add_element parent=<id> tag=<name>` — добавить элемент
- `xml.set_attribute element=<id> name=<attr> value=<val>` — установить атрибут
- `xml.get data=<id> xpath=<expr>` — получить XPath результат
- `xml.write data=<id> file=<path>` — сохранить XML

---

### 4. **registry** — Windows реестр (удобные алиасы)
Улучшенная работа с реестром с дружественными дефолтами.

```wflow
registry.set key="Software\MyApp\Settings" value="Version" data="1.0"
registry.get key="Software\MyApp\Settings" value="Version" var=VER
registry.exists key="Software\MyApp" var=EXISTS
registry.delete key="Software\MyApp\Settings" value="Version"
```

**Команды:**
- `registry.set key=<path> value=<name> data=<val> [hive=HKCU] [type=STRING]`
- `registry.get key=<path> value=<name> [hive=HKCU] [default=val]`
- `registry.exists key=<path> [hive=HKCU]`
- `registry.delete key=<path> [value=<name>]`

**Поддерживаемые hive:** HKCU (default), HKLM

---

### 5. **async** — Асинхронное выполнение задач
Запуск команд параллельно с ожиданием завершения.

```wflow
async.start command="echo Task 1" var=task1
async.start command="echo Task 2" var=task2
async.status task=${task1} var=STATUS
async.wait task=${task1}
async.wait_all tasks=${task1},${task2}
```

**Команды:**
- `async.start command=<cmd> [var=<taskId>]` — запустить задачу
- `async.wait task=<id> [timeout=<sec>]` — ждать завершения
- `async.wait_all tasks=<id1,id2,..>` — ждать все задачи
- `async.status task=<id>` — статус задачи (например: running, ranToCompletion, faulted)

---

### 6. **input** — Интерактивный ввод данных
Запрос информации у пользователя в консоли.

```wflow
input.text prompt="Enter name: " default=Guest var=NAME
input.password prompt="Password: " var=PWD
input.confirm prompt="Proceed?" default=no var=CONFIRM
input.choice prompt="Pick one" options="red,green,blue" var=COLOR
```

**Команды:**
- `input.text prompt=<msg> [default=<val>]` — текстовый ввод
- `input.password prompt=<msg>` — скрытый ввод пароля
- `input.confirm prompt=<msg> [default=yes|no]` — да/нет вопрос (по умолчанию no)
- `input.choice prompt=<msg> options=<a,b,c>` — выбор из списка

---

### 7. **import** — Модули и переиспользуемые функции
Загрузка функций из внешних скриптов без выполнения верхнеуровневого кода.

```wflow
import path=lib.wflow

define helper(x):
    echo Processing ${x}

# В другом скрипте:
import path=lib.wflow
call name=helper x=data
```

**Команды:**
- `import path=<script.wflow>` — импортировать функции из файла

**Особенности:**
- Верхнеуровневые команды НЕ выполняются
- Только функции (`define`) регистрируются
- Идеально для создания библиотек

---

## 🔄 Улучшения существующих модулей

### **datetime**
Добавлена поддержка:
- Кастомных форматов: `datetime.now format=yyyy-MM-dd`
- Парсинга дат: `datetime.parse text="2024-12-31 23:59:59"`
- Вычисления разности: `datetime.diff start="2024-01-01" end="2024-12-31" unit=days`

### **path**
Новые операции:
- Получение имени файла: `path.basename path=/folder/file.txt`
- Расширение: `path.extension path=file.txt`
- Слияние путей: `path.join parts=/root,/child`

### **log**
Структурированное логирование:
- `log.config level=INFO file=log.txt format="[%TIME%] %LEVEL% - %MESSAGE%"`
- `log.info message=<msg>`
- `log.debug message=<msg>` (при level=DEBUG или --verbose)
- `log.warning message=<msg>`
- `log.error message=<msg>`

### **isset**
Улучшена проверка переменных (сохранилось из v0.2.2)

---

## 🏗️ Архитектурные изменения

### Object Store
- Все сложные объекты (конфиги, CSV, XML) хранятся в `ExecutionContext.ObjectStore`
- Безопасная изоляция между запусками
- Чистка после завершения скрипта

### Асинхронный реестр
- `ExecutionContext.AsyncTasks` — Dictionary для управления фоновыми задачами
- Поддержка ожидания и проверки статуса
- Graceful shutdown при завершении контекста

---

## 🧪 Новые тесты

Полный набор тестовых скриптов в папке `/tests`:

```
tests/
├── test-config.wflow          # INI конфигурации
├── test-csv.wflow             # CSV работа
├── test-xml.wflow             # XML документы
├── test-registry.wflow        # Windows реестр
├── test-async.wflow           # Асинхронные задачи
├── test-input.wflow           # Интерактивный ввод
├── test-import.wflow          # Импорт модулей
├── import-lib.wflow           # Пример библиотеки
└── test-master-inputs.wflow   # Интерактивный раннер тестов
```

---

## 🔐 Безопасность

- **Доступ к реестру**: HKCU по умолчанию (безопаснее HKLM)
- **Пароли**: скрываются при вводе (`input.password`)
- **Файлы**: допускаются абсолютные и относительные пути; ответственность за безопасность путей на авторе скрипта

---

## 🚀 Примеры использования

### Полный workflow: создание и обработка конфига
```wflow
# Создать конфиг
config.create var=APP_CFG
config.set config=${APP_CFG} section=Database key=Host value=localhost
config.set config=${APP_CFG} section=Database key=Port value=5432
config.write config=${APP_CFG} file=app.ini

# Прочитать и вывести
config.read file=app.ini var=APP_CFG
config.get config=${APP_CFG} section=Database key=Host var=DB_HOST
echo Database host: ${DB_HOST}
```

### Асинхронные фоновые задачи
```wflow
async.start command="process.exec file=wget args=https://example.com/data.zip" var=download
async.start command="process.exec file=7z args=x archive.zip" var=extract

echo Waiting for downloads...
async.wait task=${download}
echo Download complete!
async.wait task=${extract}
echo All done.
```

### Импорт функций для переиспользования
```wflow
import path=shared-lib.wflow
define main():
    call name=setup_database
    call name=validate_config
    echo All checks passed!

call name=main
```

---

## 📊 Сравнение версий

| Модуль | v0.2.2 | v0.2.3 |
|--------|--------|--------|
| config | ❌ | ✅ |
| csv | ❌ | ✅ |
| xml | ❌ | ✅ |
| registry | ❌ (только reg.get/set) | ✅ (улучшено) |
| async | ❌ | ✅ |
| input | ❌ | ✅ |
| import | ❌ | ✅ |
| datetime | ✅ | ✅ (улучшено) |
| path | ✅ | ✅ (расширено) |
| log | ✅ | ✅ (улучшено) |

---

## 🔗 Установка и обновление

Скачайте последнюю версию: https://github.com/silasWorked/WinFlow/releases/tag/v0.2.3

Или обновитесь через CLI:
```bash
winflow update
```

---

## 📝 Известные ограничения

- **CSV**: только простые текстовые таблицы (без кавычек и спецсимволов в полях)
- **XML**: базовая поддержка (нет XSD валидации)
- **registry**: требует HKCU доступ (HKLM может потребовать админ права)
- **async**: максимум ~100 одновременных задач (системное ограничение)

---

## 🤝 Обратная связь

Обнаружили баг? Есть идеи? Создайте issue на GitHub:
https://github.com/silasWorked/WinFlow/issues

---

**WinFlow Team** 🚀
